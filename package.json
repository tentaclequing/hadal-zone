{
  "name": "hadal-zone",
  "version": "1.0.0",
  "private": true,
  "description": "Build tools and CI scripts for hadal-zone",
  "scripts": {
    "test": "jest",
    "test:frontmatter": "jest scripts/tests/validate-frontmatter.test.js",
    "validate:frontmatter": "node scripts/validate-frontmatter.js",
    "build:search": "pagefind --site public"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "pagefind": "^1.3.0",
    "js-yaml": "^4.1.0"
  }
}
